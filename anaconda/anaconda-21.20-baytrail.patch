--- anaconda/pyanaconda/bootloader.py	2014-02-07 12:14:49.243550082 -0800
+++ anaconda/pyanaconda/bootloader.py.new	2014-02-07 13:11:05.173906921 -0800
@@ -1625,7 +1625,7 @@
         return ret
 
 class EFIGRUB(GRUB2):
-    packages = ["grub2-efi", "efibootmgr", "shim"]
+    packages = ["grub2-efi", "efibootmgr"]
     can_dual_boot = False
     stage2_is_valid_stage1 = False
     stage2_bootable = False
@@ -1637,6 +1637,7 @@
     def __init__(self):
         super(EFIGRUB, self).__init__()
         self.efi_dir = 'BOOT'
+        self.boot_args.add("video=800x1280@75")
 
     def efibootmgr(self, *args, **kwargs):
         if kwargs.pop("capture", False):
@@ -1690,7 +1691,7 @@
         rc = self.efibootmgr("-c", "-w", "-L", productName,
                              "-d", boot_disk.path, "-p", boot_part_num,
                              "-l",
-                             self.efi_dir_as_efifs_dir + "\\shim.efi")
+                             self.efi_dir_as_efifs_dir + "\\grubia32.efi")
         if rc:
             raise BootLoaderError("failed to set new efi boot target")
 
